<script lang="ts">import {page} from '$app/state';let {data,children}=$props();const links=[['/','Overview','◫'],['/sell','Sell Internet','＋'],['/vouchers','Vouchers','▤'],['/voucher-batches','Voucher Batches','▦'],['/packages','Packages','◇'],['/sales','Sales','↗'],['/access-grants','Access Grants','◎'],['/sessions','Wi-Fi Sessions','⌁'],['/reports','Reports','▥'],['/staff','Staff','♙'],['/settings','Network setup','⚙']];const cashier=['/','/sell','/vouchers','/packages','/sales'];</script>
<div class="workspace"><aside class="sidebar"><a class="brand" href="/">◉ <span>{data.brand}</span></a><p class="sidebar-label">SHOP WORKSPACE</p><nav aria-label="Main navigation">{#each links as [url,label,icon]}{#if data.staff.role==='ADMIN'||cashier.includes(url)}<a href={url} class:current={page.url.pathname===url}><span aria-hidden="true">{icon}</span>{label}</a>{/if}{/each}</nav><div class="staff-summary"><strong>{data.staff.display_name}</strong><span>{data.staff.role==='ADMIN'?'Administrator':'Cashier'}</span><form method="POST" action="/logout"><button class="text-button">Sign out ↗</button></form></div></aside><div class="workspace-main"><header class="topbar"><span>One shop. Everything in view.</span><span class="badge">TZS · Tanzania</span></header>{#if data.mode!=='live'}<div class="simulation" role="status"><strong>SIMULATION MODE</strong> Vouchers and sales are real records. Voucher authorization is simulated. Router diagnostics still contact the configured router.</div>{/if}<main class="content">{@render children()}</main><footer class="workspace-footer">{data.brand} <span>Prepaid internet, made simple.</span></footer></div></div>
