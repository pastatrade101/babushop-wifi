<script lang="ts">let {data}=$props();const money=(n:number)=>new Intl.NumberFormat('en-TZ').format(n||0);</script>
<svelte:head><title>Overview · {data.brand}</title></svelte:head>
<div class="page-heading"><div><p class="eyebrow">YOUR SHOP AT A GLANCE</p><h1>Good connections start here.</h1><p>Today’s activity, with the details that matter.</p></div><a href="/sell" class="button">＋ Sell Internet</a></div>
{#if data.staff.role==='ADMIN'}<section class="metrics financial"><article><span>Gross cash sales today</span><strong><small>TZS</small> {money(data.metrics.gross_tzs)}</strong></article><article><span>Reversals today</span><strong><small>TZS</small> {money(data.metrics.reversals_tzs)}</strong></article><article><span>Net cash sales today</span><strong><small>TZS</small> {money(data.metrics.net_tzs)}</strong></article></section>{/if}
<section class="metrics"><article><span>Available stock</span><strong>{data.metrics.stock}</strong><small>Generated and not yet sold</small></article><article><span>Sold, unused</span><strong>{data.metrics.sold_unused}</strong><small>Validity has not started</small></article><article><span>Active entitlements</span><strong>{data.metrics.active}</strong><small>Not a count of online devices</small></article><article><span>Needs review</span><strong>{data.metrics.needs_review}</strong><small>Authorization needs attention</small></article></section>
{#if data.integration}<section class="panel"><div class="print-toolbar"><div><p class="eyebrow">CONNECTION SERVICE</p><h2>Omada · {data.integration.mode==='mock'?'Simulation':'Live'}</h2></div><a class="small-button" href="/settings">Integration settings →</a></div><p>{data.metrics.vouchers_sold||0} vouchers sold today · Last operator check: {data.integration.last_check?new Intl.DateTimeFormat('en-TZ',{dateStyle:'medium',timeStyle:'short',timeZone:'Africa/Dar_es_Salaam'}).format(new Date(data.integration.last_check)):'Not checked'}</p></section>{/if}<div class="split"><section class="panel"><p class="eyebrow">THE EVERYDAY FLOW</p><h2>From your counter to a connection</h2><ol class="steps"><li><strong>Prepare voucher stock</strong><p>An administrator generates codes for each package.</p></li><li><strong>Record the cash sale</strong><p>Reserve stock, confirm cash received, then print.</p></li><li><strong>Let the customer connect</strong><p>They join Wi-Fi and enter their voucher. Time starts at activation.</p></li></ol></section><section class="panel accent"><p class="eyebrow">KEEP IT SIMPLE</p><h2>One voucher.<br>One device.</h2><p>Time continues while a customer is disconnected. Reconnecting never restarts the package.</p><a href="/vouchers" class="button secondary">View voucher stock →</a>{#if data.staff.role==='ADMIN'}<a href="/settings" class="text-link">Check Omada integration →</a>{/if}</section></div>
